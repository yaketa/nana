# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code への指示書です。
利用者にも読めるよう、平易な日本語で書いています。

## 利用者と役割分担

- 利用者はソフトウェア開発の素人で、日本語話者。スマホから操作していることが多い。
  専門用語は通じない前提で、かみ砕いて話す
- 利用者の仕事: 何を作るか決める / できたものを触る / 違和感を伝える / PR をマージする
- Claude の仕事: それ以外の全部（設計・実装・調査・検証・Git 操作・わかりやすい説明）
- 「●●が変」「なんか気持ち悪い」のような曖昧な報告が、ここでの正常な入力。
  詳しい報告やログの提出を利用者に要求しない

## 曖昧な報告への対応

- **症状は信じる。診断は検証する。** 「たぶん××のせい」と言われても、
  それは仮説として扱い、裏を取ってから直す
- 手順: 自分で再現・観察 → 根本原因の特定 → 修正 → 動作確認 → 報告。
  表面だけ取り繕う対症療法にしない
- 報告は平易な日本語で 3 点セット: ① 原因 ② 変えたこと ③ どう確認したか
- 自分の環境で再現できないときは、再現に必要なもの（見えている画面、
  出たメッセージなど）を 1 つだけ具体的に頼む
- どうしても行き詰まったときだけ質問する。その場合も「どこが変ですか？」ではなく、
  具体的な候補を並べてタップで選べる形式で 1 問だけ
- 例外: 見た目や使い勝手の「好み」は勝手に決めず、選択肢を 2〜3 個だけ短く示して
  選んでもらう

## 疑ってよい

- 言われたとおりだと悪化する、もっと簡単な方法がある、と気づいたら
  実装前に短く代案を伝える
- 調査の結果が利用者の見立てと食い違ったら、遠慮せずはっきりそう言う
- ただし最終決定は常に利用者。決まったらそれに従う

## 完了の定義

- 「できました」と言う前に、テスト・実行・観察のいずれかで自分で動作を確認する
- 「試してみてください」を動作確認の代わりにしない。動くことの確認は Claude の仕事
- 利用者に見てもらうのは好みや使い心地だけ。その際は「ここまで確認済み」と
  「ここを見てほしい」を分けて伝える
- テスト基盤ができてからは、バグを 1 つ直すたびに再発防止のテストを 1 件添える

## Git と PR

- デフォルトブランチは main。main に直接 push しない
- 作業はブランチで行い PR を出す。説明は平易な日本語で「何を / なぜ / どう確認したか」
- CI が緑になったのを確認してから PR のリンクを渡す。利用者の操作がマージボタン
  1 つで済むようにする
- 1 つの PR は 1 つの目的。ついでのリファクタリングや無関係な変更を混ぜない
- 「戻して」と言われたら、どの変更を戻すのか特定して revert の PR を作る。
  Git 操作を利用者にさせない
- 作業開始時と PR 作成前に git fetch でリモートの現状を確認してから判断する。
  ローカルの参照は古くなっていることがある
- 他の claude/* ブランチや未マージの PR を見つけたら、中身を確認し、
  勝手にマージも削除もせず、利用者に報告して扱いを決める

## 記録

- 利用者が決めたこと（スタック、名前、仕様の選択など）は下の「決定事項」に
  1 行ずつ追記し、その作業と同じ PR に含める。目的は、次のセッションが
  同じ質問を繰り返さないこと
- 一覧が長くなったら整理・要約してよい

## 秘密情報

- API キーやパスワードは絶対にコミットしない。初めて必要になった時点で、
  安全な置き場所（環境変数など）を仕組みごと提案する

## スキル

- `.claude/skills/` に開発補助のスキルがある。代表は `tap`: スマホ操作時、長文入力を
  タップ選択に置き換えるモード（`/tap` のほか「移動中」「片手」などの合図でも発火）
- スキルはアプリのコードではないためビルドやテストは増えない。変えたときは実際に
  セッションで呼び出して挙動を確かめる（CI では検証されない）

## 現状

- 作っているもの: 分杭峠（ゼロ磁場）を紹介する静的ウェブサイト
- 構成: フレームワークなしの HTML + CSS。`index.html` 1 ファイルに全部入り（JS なし）。
  写真だけ `images/` に置く。ビルド工程はない。ブラウザで `index.html` を開けば
  そのまま見られる
- テスト: Node.js 標準のテストランナーを使用（追加インストール不要）
  - 全部実行: `node --test`
  - 1 件だけ実行: `node --test --test-name-pattern="名前の一部"`
- Lint: なし（必要になったら導入を検討）
- CI（.github/workflows/ci.yml）: push / PR のたびに `node --test` を実行する
- 公開: GitHub Pages。main が更新されると .github/workflows/deploy.yml が
  テスト → 公開を自動で行う。URL は https://yaketa.github.io/nana/
  ※ただしリポジトリが非公開のままだと無料プランでは Pages を使えないため、
  その間 deploy.yml は公開ステップを自動でスキップする（エラーにはならない）
- サイト内の交通情報（バスの期間・料金など）は 2026 年 8 月時点の調べ。
  シーズンが変わったら公式サイトを確認して更新する

## 決定事項

- 2026-08-07: 開発は PR ベースで回す。Claude からの質問はタップで選べる選択式を基本とし、
  説明はすべて平易な日本語で行う
- 2026-08-07: 最初の題材は「分杭峠（ゼロ磁場）の紹介サイト」。ゼロ磁場について
  学べる内容も入れ、興味がある人が行きたくなるトーンで作る
- 2026-08-07: スタックはフレームワークなしの静的 HTML/CSS（index.html 1 ファイル）。
  テストと CI は Node 標準の `node --test`
- 2026-08-07: 公開先は無料の GitHub Pages（利用者の希望は「GitHub Pages か
  Cloudflare か Vercel」。追加のアカウント登録が不要な GitHub Pages を採用）
- 2026-08-07: サイトの見た目は「るるぶ」のような旅行ガイドブック風
  （ビビッドな配色・太い丸ゴシック見出し）。基本フォントは Noto Sans JP
- 2026-08-07: 手描き風の SVG イラストは使わない。写真・図解が入る場所は
  プレースホルダーの枠にしておき、あとで実物に差しかえる
- 2026-08-07: プレースホルダーの枠 3 つに、Wikimedia Commons の自由に使える
  現地写真を入れた（分杭峠の標識／北川露頭／安康露頭）。写真は `images/` に
  枠の形に切り抜いて置き、大小 2 サイズを `srcset` で出し分ける。
  CC BY / CC BY-SA なので、写真ごとのキャプションとフッターの両方に
  「撮影者・ライセンス・切り抜いた旨」を必ず書く。差しかえるときも同じ扱いにする
- 2026-08-07: 「あわせて寄りたい」の 4 枚のカードは、広い画面できっちり 2×2 にならべる
  （せまい画面では 1 列）。カードの上辺いっぱいに 16:9 の写真を敷き、そのすぐ下に
  小さな出典を 1 行つける。写真はここも Wikimedia Commons の自由に使えるものを使い、
  `images/` に 480 と 960 の 2 サイズを置いて `srcset` で出し分ける
- 2026-08-07: 「行き方」の料金（往復 大人／小学生／駐車場）は、3 枚のカードの中ではなく
  カードの下に「きっぷ」の形で横ならびに置く。カードは中身の量がそろっていないと
  短いほうの下に空白ができるため、量の偏る要素はカードの外に出す
- 2026-08-07: バス時刻表は 2 列の表のまま質を上げる。＊印はやめ、該当便の時刻の横
  （せまい画面では下）に小さな「土休日のみ」ラベルを添える。しま模様の背景はやめ、
  凡例は表の下の注記に 1 文で書く
- 2026-08-07: ヘッダーのメニューは、せまい画面ではハンバーガー式（「メニュー」ボタン
  →タップでパネルが開く）。JS は使わず URL の #（:target）で開閉し、とじるリンクは
  飛び先のない「#_」を使う。広い画面は従来どおりチップ 1 行。候補として
  「1行チップ＋続きあり矢印」「折り返して全部表示」も画像で提示した上で利用者が選択
- 2026-08-07: 内容の幅はサイト全体で 1 本の基準（CSS 変数 --page＝48rem）にそろえる。
  基準は本文の読みやすい行長（1 行 全角 44 字ほど）。見出し帯・写真・表・カードも
  同じ幅にし、部品ごとに幅を変えて本文の横に大きな空白を作らない。2 列ならびで
  最後の 1 個が半端にあまるときは全幅にのばす（例外は表紙の紹介パネルのみ）
- 2026-08-07: 「やすっぽい」との報告への対応は、表紙の標高バッジ 1 点のみ（利用者が選択）。
  ギザギザ星形は保ち、トゲを「16 本・深さふつう」の等間隔にして、インキの縁取りを追加
  （数と深さは 16 種の一覧表から利用者が選択）。作りはインライン SVG で、塗り・線の色は
  CSS の色トークン。黄色・位置・回転・水玉背景・題字の赤ズレなど、ほかの飾りは現状維持
- 2026-08-07: SVG はデザイン用途で適切に使ってよい（「手描き風の SVG イラストは使わない」
  の決定を置きかえ。JS を使わない方針は変わらない）。第 1 号は表紙の標高バッジ
- 2026-08-07: ふりがな（ルビ）は position / display をさわらない。いちど「絶対配置で
  題字に貼りつける」やり方を入れたところ、iPhone（Safari）ではふりがながルビとして
  扱われなくなり、題字の右へ流れ出て並んだ（PR #21、翌 PR で撤回）。Safari のルビは
  rt を専用の部品として組む作りで、position を付けるとその部品から外れ、ただの文字に
  なってしまう。配置自体はブラウザ標準のルビに任せ、その中で決められる余白
  （line-height / margin / padding）だけを数値で言い切る。単位は題字の文字サイズ
  基準（em）にする
- 2026-08-07: 端末差の直し方には順番がある。まず「言い忘れてブラウザの初期値が入る」
  すきまを埋める（余白や行の高さを 0 や数値で言い切る）。ブラウザ標準のしくみ自体を
  置きかえるのは最後の手段で、置きかえる場合は Chrome 以外でも壊れないことを
  確かめてからにする。手元で再現できない症状のときは、まず小さく安全な側に倒す
  （同種の前例: 題字の赤い版ズレは text-shadow → filter で解決している）
- 2026-08-07: 表紙のふりがなは、iPhone のときだけ 1.05em ぶん題字へ引き寄せる
  （`@supports (-webkit-touch-callout:none)` の中で rt に負の margin-bottom）。
  余白と行の高さをそろえてもなお iPhone だけ離れて見えたため。残る差は
  「ルビの箱を題字のどれだけ上に置くか」というブラウザ内部の判断で、CSS からは
  指定できない。同じ値を全ブラウザに当てると Chrome では題字と重なるので、
  対象は iPhone に限定する
- 2026-08-07: 手元で再現できない見た目は、スクリーンショットを見せて選ばせない。
  こちらの Chrome で描いた画像は、書体もブラウザもちがうので比較材料にならない
  （利用者からの指摘）。近さや大きさの候補を並べた一時ページをサイトに公開し、
  実機で見て選んでもらう。そのとき候補 A は「いま公開されているもの」にして、
  本物と見くらべれば見本が正しいか利用者自身が確かめられるようにする。
  役目が終わったら一時ページは必ず消す（消し忘れ防止のテストあり）
- 2026-08-08: 分杭峠の読みは「ぶんくいとうげ」（く＝濁点なし）。現地の道路標識
  （長野県設置）が "Bunkuitoge"、伊那市公式ページの見出しが「分杭峠（ぶんくいとうげ）」、
  日本歴史地名大系も「ぶんくいとうげ」。世間には「ぶんぐい」も多く出回っており
  （Wikipedia もこちら）混ざりやすいので、書きたすときは必ず濁点なしにする。
  自分たちで名前を決められるもの（写真のファイル名など）も同じ。
  外部サイトの URL は先方の綴りのまま変えない（再発防止のテストあり）

- 2026-08-08: 写真の出典（撮影者・ライセンス）は、写真の下ではなく写真の右下に重ねて置く。
  ライセンス上まず消せない表記だが、本文の下に並ぶと読みものの流れを切るため（利用者の判断）。
  写真は明るいものも暗いものもあるので、インキ色の下敷き（濃さ .72）を敷いて字を白く抜く
  ——まっ白な写真に重なった最悪の場合でもコントラスト比が約 5:1 になる値で、
  ここを薄くすると小さい字が読めなくなる。長さを抑えるため「（切り抜いて使用）」は
  「（切り抜き）」に短縮した（切り抜いた旨を書く方針は変わらない）。
  作りは写真を `div.shot-pic` で包み、その中に `p.credit` を絶対配置。
  CSS のセレクタは `.shot-pic .credit`（`.credit` 単体だとカードの中で `.card p` に負け、
  出典だけ本文と同じ大きさに組まれて写真の半分を覆う）。再発防止のテストあり

## この指示書について

- この指示書自体が邪魔になっていると感じたら、黙って従わず、削る・緩める提案をする
- ルールは増やすより減らす方を優先する

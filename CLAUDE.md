# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの現状

`nana` は、まだ中身がほとんどない初期状態のリポジトリです。追跡されているファイルは次の 2 つだけです。

- `README.md` — 見出し `# nana` の 1 行のみ
- `.github/workflows/ci.yml` — CI の雛形

つまり、**アプリケーションのコード・依存パッケージ・テストはまだ 1 つも存在しません**。
言語もフレームワークもパッケージマネージャも未定で、`package.json` や `pyproject.toml` に
あたるファイルもありません。

そのため、このファイルにはビルド・Lint・テストのコマンドをまだ書けません。
存在しないコマンドを推測して実行しないでください。

## CI

`.github/workflows/ci.yml` は `main` への push と、すべての Pull Request で動きます。
ジョブは `ready` の 1 つだけで、中身は `echo ready` を実行するだけの置き石です。
検証は何もしていないので、CI が緑でもコードが正しいことの証明にはなりません。

実際のビルドやテストを追加したときは、このワークフローにもステップを足してください。

## 最初にコードを追加するときの注意

技術スタックを決めてコードを入れる作業は、このリポジトリでは「既存の書き方に合わせる」
という判断ができない最初の一歩になります。以下を意識してください。

- 言語やフレームワークの選択は影響が大きいので、指示がなければ利用者に確認する
- スタックを決めたら、このファイルの「このリポジトリの現状」を実際の構成に置き換え、
  ビルド・Lint・テストの実行コマンド（単体テスト 1 件だけを走らせる方法を含む）を追記する
- `.github/workflows/ci.yml` のプレースホルダを、実際に検証を行うステップへ差し替える

## Git

デフォルトブランチは `main` です。
